name: CI - Go

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: emilgruzalski/gh-reusable-workflows/.github/workflows/ci-go.yml@5eab2fe299653adaf03b793b7cc9c45f882bbd24 # v1.1.0
    with:
      image-name: 'zguba-gov'
      go-version-file: 'go.mod'
      working-directory: '.'
      dockerfile: './Dockerfile'
      context: '.'
      push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
    permissions:
      contents: write
      packages: write
      security-events: write
      id-token: write